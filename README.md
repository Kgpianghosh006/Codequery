Codequery
A simple web-based application designed to query and retrieve code-related solutions or questions from a local dataset.

🚀 Features
Search Functionality: Filter through the dataset to find specific programming solutions.

Flask Backend: A lightweight Python server to handle queries.

JSON Data: Uses a structured .json file as the primary data source.

Simple UI: A clean interface for entering queries and viewing results.

🛠️ Tech Stack
Backend: Python, Flask

Frontend: HTML, CSS

Data: JSON

📋 Prerequisites
Ensure you have Python installed on your system. You will also need the following library:

Flask

⚙️ Installation & Setup
Clone the repository:

Bash

git clone https://github.com/Kgpianghosh006/Codequery.git
cd Codequery
Install dependencies:

Bash

pip install -r requirements.txt
Run the application:

Bash

python app.py
Access the app: Open your browser and go to http://127.0.0.1:5000

📁 Project Structure
app.py: The main Flask application script.

questions.json: The dataset containing code queries and answers.

templates/: Contains the HTML files for the frontend.

static/: Contains CSS and styling assets.
